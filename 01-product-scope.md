# 01 — Product Scope / Launch Lock

## 한 문장
**교체뚝딱 — 이거, 언제 바꿔야 하지?**

Google·Naver의 `[품목] 교체주기/교체시기` 검색자가
10초 안에 정확한 교체 기준을 이해하고,
필요하면 관리 → 재구매 → 기존 제품 폐기까지 이어가는 생활도구 사이트.

## 사업 흐름
Organic Search
→ AnswerCard
→ 품목별 Tool
→ AdSense
→ 호환 가능한 경우 Coupang
→ 정확한 폐기 페이지가 있으면 버림뚝딱

## Launch 데이터
- Seed 48개
- indexable Launch 15개
- 나머지는 `needs_research`
- 신규 확장 후보 111개는 페이지 생성 대상이 아니라 조사 후보

## Launch에서 구현
`10-feature-flags.json`의 `launch`만 구현.

중요:
- `관리했어요`는 지원 품목만
- 모델/차종 입력은 localStorage만
- 사용횟수/주행거리도 사용자가 직접 입력한 값으로만 계산

## Post-launch
빠른 루틴 담기, 최근 본 품목, Web Share, missing-search D1,
첫 모델별 SEO 페이지는 **배포 뒤 실제 사용자 데이터와 구현 품질을 확인한 다음**.

## 계속 제외
계정, 가족공유, 사진 AI, 커뮤니티, 전체 이력/통계, R2 의존,
동적 OG Worker, 100페이지 AI 자동발행.

## 핵심 품질 원칙
1. 검색어는 사용자의 언어로 잡고 답은 사실대로 쓴다.
2. 모든 물건에 고정 `N개월`을 만들지 않는다.
3. 모델/차종 호환이 확인되지 않으면 상품을 추천하지 않는다.
4. 광고보다 답과 도구가 먼저다.
5. Codex는 콘텐츠 생성기가 아니라 검증된 데이터의 구현자다.
