(()=>{const el=document.querySelector('#today-visitors');if(!el)return;const parts=Object.fromEntries(new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Seoul',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts().map(x=>[x.type,x.value]));const day=`${parts.year}-${parts.month}-${parts.day}`,key='gyochettukttak:today-counted';let method='GET';try{if(sessionStorage.getItem(key)!==day){sessionStorage.setItem(key,day);method='POST'}}catch{}fetch('/api/today-visitors',{method,headers:{accept:'application/json'},cache:'no-store',credentials:'omit'}).then(r=>r.ok?r.json():Promise.reject()).then(x=>{if(/^\d{4}-\d\d-\d\d$/.test(x.date)&&Number.isSafeInteger(x.count))el.textContent=`TODAY ${x.count.toLocaleString('en-US')}`}).catch(()=>{})})();
